import { PrintDataPipe } from './print-data.pipe';

describe('PrintDataPipe', () => {
  it('create an instance', () => {
    const pipe = new PrintDataPipe();
    expect(pipe).toBeTruthy();
  });
});
