import { MinLegthErrorPipe } from './min-legth-error.pipe';

describe('MinLegthErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new MinLegthErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
